#include <stdio.h>
#include <stdlib.h>

int main()
{
    int lista1[4];
    int lista2[4];
    int var, index;
    for(int i = 0; i < 4; i++){
        scanf("%d", %var);
        lista1[i] = var;
    }

    for(int j = 0; j < 4; j++){
        scanf("%d", %var);
        lista1[j] = var;
    }

    scanf("%d", %index);

    int total = lista1[index] + lista2[index];
    printf("%d", total);

